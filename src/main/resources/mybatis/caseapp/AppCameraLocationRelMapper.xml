<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.caseapp.location.mapper.AppCameraLocationRelMapper">

    <select id="selectCameraIdsByLocationId" parameterType="Long" resultType="Long">
        select camera_id
        from app_camera_location_rel
        where location_id = #{locationId}
    </select>

    <select id="selectLocationIdsByCameraId" parameterType="Long" resultType="Long">
        select location_id
        from app_camera_location_rel
        where camera_id = #{cameraId}
    </select>

    <select id="selectCameraQymcByLocationId" parameterType="Long" resultType="String">
        select distinct r.gnslx
        from app_camera_location_rel rel
        inner join app_roomip r on rel.camera_id = r.id
        where rel.location_id = #{locationId}
        and r.gnslx is not null
    </select>

</mapper>

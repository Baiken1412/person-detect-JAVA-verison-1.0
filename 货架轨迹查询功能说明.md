# 基于货架位置的人员活动轨迹查询功能

## 功能概述
实现根据货架位置查询人员活动轨迹的功能,通过货架与摄像头的映射关系,查询指定时间段内某货架区域的人员活动记录。

## 实现内容

### 1. 数据库设计
**表名**: `shelf_camera_mapping`
**文件**: `sql/shelf_camera_mapping.sql`

**字段说明**:
- `id`: 主键ID
- `shelf_code`: 货架编码(如: A01-03-02)
- `shelf_name`: 货架名称(如: 东区货架A组)
- `area_code`: 所属区域编码(如: A区)
- `area_name`: 所属区域名称
- `camera_id`: 关联摄像头ID
- `camera_name`: 摄像头名称
- `qymc`: 对应的检测区域名称(用于匹配app_track表)

**测试数据**:
已插入6条测试数据,映射了A/B/C三个区域的6个货架位置到对应的检测区域。

### 2. 后端实现

#### 实体类
- **ShelfCameraMapping**: 货架-摄像头映射实体
  - 位置: `src/main/java/com/ruoyi/project/caseapp/shelf/domain/ShelfCameraMapping.java`

#### Mapper层
- **ShelfCameraMappingMapper**: 数据访问接口
  - 位置: `src/main/java/com/ruoyi/project/caseapp/shelf/mapper/ShelfCameraMappingMapper.java`
  - XML: `src/main/resources/mybatis/caseapp/ShelfCameraMappingMapper.xml`
  - 方法:
    - `selectShelfCameraMappingList`: 查询货架映射列表
    - `selectByShelfCode`: 根据货架编码查询
    - `selectByAreaCode`: 根据区域编码查询

#### Service层
- **IShelfTrajectoryService**: 服务接口
  - 位置: `src/main/java/com/ruoyi/project/caseapp/shelf/service/IShelfTrajectoryService.java`

- **ShelfTrajectoryServiceImpl**: 服务实现
  - 位置: `src/main/java/com/ruoyi/project/caseapp/shelf/service/impl/ShelfTrajectoryServiceImpl.java`
  - 方法:
    - `queryTrajectoryByShelf`: 根据货架位置查询轨迹
    - `getShelfList`: 获取货架列表(支持按区域过滤)
    - `getAreaList`: 获取区域列表

#### Controller层
- **ShelfTrajectoryController**: 控制器
  - 位置: `src/main/java/com/ruoyi/project/caseapp/shelf/controller/ShelfTrajectoryController.java`
  - 访问路径: `/caseapp/shelf`
  - 端点:
    - `GET /trajectory`: 跳转到查询页面
    - `POST /queryTrajectory`: 查询轨迹数据
    - `GET /getShelfList`: 获取货架列表
    - `GET /getAreaList`: 获取区域列表

### 3. 前端实现
**页面**: `src/main/resources/templates/caseapp/shelf/trajectory.html`

**功能特性**:
- 下拉选择货架位置(动态加载)
- 区域联动筛选
- 时间范围选择(支持datetime-local)
- 轨迹列表展示:
  - 轨迹编号
  - 货架位置信息
  - 关联摄像头
  - 活动时间段和时长
  - 关键帧图片
  - 视频截图时间轴
  - 操作按钮(播放视频、查看事件)

**API集成**:
- 页面加载时自动获取货架和区域列表
- 区域选择时自动过滤货架列表
- 提交查询时调用后端API获取真实数据

## 使用说明

### 1. 数据库初始化
```sql
-- 执行建表和测试数据插入
source sql/shelf_camera_mapping.sql;
```

### 2. 访问页面
```
URL: http://localhost:8099/caseapp/shelf/trajectory
```

### 3. 操作流程
1. 页面自动加载区域和货架列表
2. 选择货架位置(必填)
3. 可选:选择区域进行过滤
4. 选择开始和结束时间
5. 点击"查询轨迹"按钮
6. 查看查询结果

## 核心逻辑

### 查询流程
1. 前端传递货架编码和时间范围
2. 后端根据货架编码查询对应的检测区域名称(qymc)
3. 使用检测区域名称和时间范围查询app_track表
4. 将查询结果转换为前端需要的格式返回
5. 前端渲染轨迹卡片列表

### 数据映射关系
```
货架编码 (shelf_code)
    ↓
货架-摄像头映射表 (shelf_camera_mapping)
    ↓
检测区域名称 (qymc)
    ↓
轨迹表 (app_track)
    ↓
轨迹结果
```

## 扩展建议

### 当前实现
- 支持单货架查询
- 支持区域联动过滤
- 展示轨迹基本信息和关键帧

### 可扩展功能
1. 批量货架查询
2. 视频播放功能集成
3. 轨迹详情查看(跳转到事件详情页)
4. 数据导出(Excel/PDF)
5. 统计图表(活动频率、时段分析)
6. 分页加载(当前一次加载所有结果)

## 测试建议

### 测试数据
1. 确保app_track表中有对应区域的轨迹数据
2. 测试数据中的qymc字段值应与实际检测区域名称匹配
3. 建议的测试区域:
   - 进门
   - 枪械库外门
   - 枪械库
   - 枪械库内门
   - 卷宗库
   - 卷宗库内门

### 测试场景
1. 查询有数据的货架和时间段
2. 查询无数据的货架和时间段
3. 测试区域联动过滤
4. 测试时间范围筛选
5. 测试结果展示和交互

## 注意事项

1. **时区处理**: 前端datetime-local使用本地时间,后端转换为数据库时间格式
2. **性能优化**: 大量数据时建议添加分页功能
3. **权限控制**: 根据需要在Controller添加权限注解
4. **错误处理**: 已添加基本异常捕获和日志记录
5. **数据准确性**: 确保货架-摄像头映射关系正确配置

## 文件清单

### SQL文件
- `sql/shelf_camera_mapping.sql` - 建表和测试数据

### Java文件
- `src/main/java/com/ruoyi/project/caseapp/shelf/domain/ShelfCameraMapping.java`
- `src/main/java/com/ruoyi/project/caseapp/shelf/mapper/ShelfCameraMappingMapper.java`
- `src/main/java/com/ruoyi/project/caseapp/shelf/service/IShelfTrajectoryService.java`
- `src/main/java/com/ruoyi/project/caseapp/shelf/service/impl/ShelfTrajectoryServiceImpl.java`
- `src/main/java/com/ruoyi/project/caseapp/shelf/controller/ShelfTrajectoryController.java`

### 配置文件
- `src/main/resources/mybatis/caseapp/ShelfCameraMappingMapper.xml`

### 前端文件
- `src/main/resources/templates/caseapp/shelf/trajectory.html`

---
**创建时间**: 2025-12-31
**版本**: 1.0
